language: rust
rust:
  - nightly-2019-10-28
sudo: false
cache: cargo

before_script:
- rustup component add rustfmt
- rustup target add wasm32-unknown-unknown
script:
  - cargo fmt --all -- --check
  - bash ./.travis.build.sh

