[workspace]
members = [
    "ontio-std",
    "ontio-codegen",
    "examples",
    "examples/token",
    "examples/token-codegen",
    "examples/oep5token-codegen",
]

[profile.release]
panic = "abort"
lto = true
debug = true
opt-level = "s"

